run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gofmt
    - goimports
    - gocritic

linters-settings:
  goimports:
    local-prefixes: github.com/iPFSoftwares/custos
